This directory contains the system level requirements for
using this environment.

bindep.txt lists the system packages needed for the system.
python-requirements.txt the python pypi packages to install.
