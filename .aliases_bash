#!/bin/bash

cdl() { cd "$@" && ls -lhFA; }

if [ "$OS" = "mac" ]; then
  alias ll='ls -lhFA'
else
  alias ll='ls -lhFA --color=auto'
  alias pbcopy='xclip -selection clipboard'
  alias pbpaste='xclip -selection clipboard -o'
fi

WORK_VAG_NET="192.168.121.0/24"
WORK_VAG_ARD_NET="192.168.245.0/24"
WORK_DE_NET="10.160.224.0/24"
HOME_NET="192.168.1.0/24"

alias devtun="sshuttle -e 'ssh -X' -r waboring@10.84.128.34 ${WORK_VAG_NET} ${WORK_VAG_ARD_NET} ${WORK_DE_NET}"
alias hometun="sshuttle -e 'ssh -X' -r waboring@hemna.mynetgear.com ${HOME_NET}"
